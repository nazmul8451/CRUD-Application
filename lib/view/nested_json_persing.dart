import 'package:flutter/material.dart';
class NestedJsonPersing extends StatelessWidget {
  const NestedJsonPersing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Nested Json Parsing',style: TextStyle(fontSize: 19,fontWeight: FontWeight.bold,color: Colors.black),),),
    );
  }
}
